# Task: Admin Provider Configuration UI

**Phase**: 2 - Admin Dashboard  
**Priority**: Medium  
**Status**: 🔄 In Progress (API Ready, UI Pending)

## Overview

Build a tabbed admin interface for configuring Morning Consult integration.
**Note**: `apps/web` is currently planned. Initial UI implementation may target `apps/docs` (Storybook) or a temporary admin panel.

## Status Update

- **Backend**: API Routes (`/api/admin/providers/*`) are implemented in `apps/worker`.
  - CRUD for Providers
  - Key Generation
  - Redirect URL configuration
- **Frontend**: Pending implementation in `apps/web`.

## UI Design

(Design specs remain valid for future implementation)

... [Rest of original content] ...
